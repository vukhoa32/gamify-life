// Copy this file to `firebase-config.js` and fill with your Firebase project's config.
// You can find these values in the Firebase console -> Project settings -> SDK setup.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

// Note: this example stores logs in the document `gamify-life/logs`.
// For production, you should secure your DB with rules and use per-user documents.
